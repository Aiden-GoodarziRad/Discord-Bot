# IMPORT DISCORD.PY. ALLOWS ACCESS TO DISCORD'S API.
import imp
import discord
import json

bot = discord.Client()

@bot.event
async def on_ready():
	guild_count = 0

	for guild in bot.guilds:
		print(f"- {guild.id} (name: {guild.name})")

		guild_count = guild_count + 1

	print("The Gaming Center Bot is in " + str(guild_count) + " guilds.")


@bot.event
async def on_message(message):
    if "game" in message.content:
        await message.channel.send("The random number is higher or lower than 76. Type higher or lower to confirm your answer!")
    if "higher" in message.content:
        await message.channel.send("Correct!")

bot.run("NOT SHOWN")
